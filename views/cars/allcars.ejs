<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cars.length %> Car<%= cars.length > 1 ? 's'  : '' %></title>
</head>
<body>
    


        <h1>All Cars</h1>
        <a href="/cars/new">Add new Car</a>

    
    <ul>
        
        <!-- i want to conditionally show no cars yet or the name of cars in db-->
        
        <% if(cars.length === 0) { %>
            <h3>No Cars Yet!</h3>
        <% } else { %>
            <% cars.forEach(car => { %>
             <li>  <a href="/cars/<%= car._id%>"> <%= car.carName %> </a> </li>
            <% }) %>
        <% } %>
        
    </ul>
</body>
<%- include('../partials/footer') %>
</html>